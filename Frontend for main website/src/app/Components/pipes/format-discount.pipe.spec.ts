import { FormatDiscountPipe } from './format-discount.pipe';

describe('FormatDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
