import { CartDropdownDirective } from './cart-dropdown.directive';

describe('CartDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CartDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
