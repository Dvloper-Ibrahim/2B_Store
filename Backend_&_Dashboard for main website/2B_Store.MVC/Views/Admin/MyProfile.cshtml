@model _2B_Store.DTO.UserProfileDto

@{
    ViewData["Title"] = "Profile";
}

@section PageTitle {
    <title>2B - My Profile</title>
}

@*<div class="container-fluid">
    <h2>Profile : @Model.UserName</h2>
    <hr />
    <h4 class="my-2"><b>First Name</b> : @Model.FirstName</h4>
    <h4 class="my-2"><b>last Name</b> : @Model.LastName</h4>
    <h4 class="my-2"><b>Email Address</b> : @Model.Email</h4>
    <h4 class="my-2"><b>First Name</b> : @Model.PhoneNumber</h4>
    <div>
        <a asp-action="edit" asp-route-id="@Model." asp-controller="account" class="btn btn-primary">Edit</a>
        <a asp-action="Index" asp-controller="Home" class="btn btn-outline-primary">Back to Home</a>
    </div>
</div>*@

@*<div class="row">*@
<div class="container-fluid">
    <div class="col-md-4">
        <div class="card">
            <h3 class="card-header">
                Profile Information
            </h3>
            <div class="card-body">
                <div class="form-group">
                    <label class="fw-bold"><h5>Username:</h5></label>
                    <p>@Model.UserName</p>
                </div>
                <div class="form-group">
                    <label class="fw-bold"><h5>First Name:</h5></label>
                    <p>@Model.FirstName</p>
                </div>
                <div class="form-group">
                    <label class="fw-bold"><h5>Last Name:</h5></label>
                    <p>@Model.LastName</p>
                </div>
                <div class="form-group">
                    <label class="fw-bold"><h5>Email:</h5></label>
                    <p>@Model.Email</p>
                </div>
                <div class="form-group">
                    <label class="fw-bold"><h5>Phone Number:</h5></label>
                    <p>@Model.PhoneNumber</p>
                </div>
            </div>
            @*            @if (User.IsInRole("Sup_Admin"))
            {*@
            <div class="card-footer">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="mt-1 btn btn-primary">Edit Profile</a>
                <a asp-action="Index" asp-controller="Home" class="mt-1 btn btn-secondary">Back to Home</a>
                <a asp-action="ChangePassword" class="mt-1 btn btn-danger">Change Password</a>
            </div>
            @*}*@
        </div>
    </div>
</div>
@*</div>*@


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        //let sideLinks = document.querySelectorAll(".side-nav");
        //sideLinks.forEach(e => e.classList.remove("active"));

        //let activeLink = document.getElementById("admins");
        //activeLink.classList.add("active");
    </script>
}
